# Place a copy of this config to ~/.aerospace.toml
# After that, you can edit ~/.aerospace.toml to your liking

# It's not necessary to copy all keys to your config.
# If the key is missing in your config, "default-config.toml" will serve as a fallback

# You can use it to add commands that run after login to macOS user session.
# 'start-at-login' needs to be 'true' for 'after-login-command' to work
# Available commands: https://nikitabobko.github.io/AeroSpace/commands
after-login-command = []

# You can use it to add commands that run after AeroSpace startup.
# 'after-startup-command' is run after 'after-login-command'
# Available commands : https://nikitabobko.github.io/AeroSpace/commands
after-startup-command = [
    # Auto-detect monitor configuration and set appropriate workspaces
    'exec-and-forget ~/.config/aerospace/monitor-setup.sh'
]

# Start AeroSpace at login
start-at-login = true

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# See: https://nikitabobko.github.io/AeroSpace/guide#layouts
# The 'accordion-padding' specifies the size of accordion padding
# You can set 0 to disable the padding feature
accordion-padding = 30

# Possible values: tiles|accordion
default-root-container-layout = 'tiles'

# Possible values: horizontal|vertical|auto
# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'

# Possible values: (qwerty|dvorak)
# See https://nikitabobko.github.io/AeroSpace/guide#key-mapping
key-mapping.preset = 'qwerty'

# Enhanced mouse behavior for multi-monitor setup
# See https://nikitabobko.github.io/AeroSpace/guide#on-focus-changed-callbacks
# See https://nikitabobko.github.io/AeroSpace/commands#move-mouse
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']
on-focus-changed = "move-mouse window-lazy-center"

# Gaps between windows (inner-*) and between monitor edges (outer-*).
# Using per-monitor configurations for different display types
[gaps]
inner.horizontal = 10
inner.vertical =   10
outer.left =       [{ monitor.main = 10 }, { monitor.external = 15 }, 10]
outer.bottom =     [{ monitor.main = 10 }, { monitor.external = 15 }, 10]
outer.top =        [{ monitor.main = 10 }, { monitor.external = 15 }, 10]
outer.right =      [{ monitor.main = 10 }, { monitor.external = 15 }, 10]

# Per-monitor workspace assignments
# This configures which workspaces appear on which monitors by default
[workspace-to-monitor-force-assignment]
1 = 'external'    # Workspace 1 on external monitor
2 = 'external'    # Workspace 2 on external monitor
3 = 'external'    # Workspace 3 on external monitor
4 = 'external'    # Workspace 4 on external monitor
5 = 'external'    # Workspace 5 on external monitor
6 = 'external'    # Workspace 6 on external monitor
7 = 'main'        # Workspace 7 on main display
8 = 'main'        # Workspace 8 on main display
9 = 'main'        # Workspace 9 on main display
10 = 'main'       # Workspace 10 on main display
S = 'main'        # Spotify workspace on main display

# Display layouts - optimized per monitor type
# Monitor-specific settings can be configured here
# Note: monitor-layout is not currently supported in AeroSpace

# 'main' binding mode declaration
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# 'main' binding mode must be always presented
[mode.main.binding]

# All possible keys:
# - Letters.        a, b, c, ..., z
# - Numbers.        0, 1, 2, ..., 9
# - Keypad numbers. keypad0, keypad1, keypad2, ..., keypad9
# - F-keys.         f1, f2, ..., f20
# - Special keys.   minus, equal, period, comma, slash, backslash, quote, semicolon, backtick,
#                   leftSquareBracket, rightSquareBracket, space, enter, esc, backspace, tab
# - Keypad special. keypadClear, keypadDecimalMark, keypadDivide, keypadEnter, keypadEqual,
#                   keypadMinus, keypadMultiply, keypadPlus
# - Arrows.         left, down, up, right

# All possible modifiers: cmd, alt, ctrl, shift

# All possible commands: https://nikitabobko.github.io/AeroSpace/commands

# You can uncomment this line to open up terminal with alt + enter shortcut
# See: https://nikitabobko.github.io/AeroSpace/commands#exec-and-forget
# alt-enter = 'exec-and-forget open -n /System/Applications/Utilities/Terminal.app'

# See: https://nikitabobko.github.io/AeroSpace/commands#layout
alt-slash = 'layout tiles horizontal vertical'
alt-comma = 'layout accordion horizontal vertical'

# Close a tile shortcut
alt-shift-q = 'close'

# See: https://nikitabobko.github.io/AeroSpace/commands#focus
alt-h = 'focus left'
alt-j = 'focus down'
alt-k = 'focus up'
alt-l = 'focus right'

# See: https://nikitabobko.github.io/AeroSpace/commands#move
alt-shift-h = 'move left'
alt-shift-j = 'move down'
alt-shift-k = 'move up'
alt-shift-l = 'move right'

# Monitor navigation shortcuts
alt-ctrl-h = 'focus-monitor left --wrap-around'
alt-ctrl-l = 'focus-monitor right --wrap-around'
alt-ctrl-j = 'focus-monitor down --wrap-around'
alt-ctrl-k = 'focus-monitor up --wrap-around'

# Move window to another monitor
alt-ctrl-shift-h = 'move-node-to-monitor left --wrap-around'
alt-ctrl-shift-l = 'move-node-to-monitor right --wrap-around'
alt-ctrl-shift-j = 'move-node-to-monitor down --wrap-around'
alt-ctrl-shift-k = 'move-node-to-monitor up --wrap-around'

# Quick workspace access
alt-1 = 'workspace 1'
alt-2 = 'workspace 2'
alt-3 = 'workspace 3'
alt-4 = 'workspace 4'
alt-5 = 'workspace 5'
alt-6 = 'workspace 6'
alt-7 = 'workspace 7'
alt-8 = 'workspace 8'
alt-9 = 'workspace 9'
alt-0 = 'workspace 10'
alt-s = 'workspace S'  # Spotify workspace

# See: https://nikitabobko.github.io/AeroSpace/commands#move-node-to-workspace
alt-shift-1 = ['move-node-to-workspace 1', 'workspace 1']
alt-shift-2 = ['move-node-to-workspace 2', 'workspace 2']
alt-shift-3 = ['move-node-to-workspace 3', 'workspace 3']
alt-shift-4 = ['move-node-to-workspace 4', 'workspace 4']
alt-shift-5 = ['move-node-to-workspace 5', 'workspace 5']
alt-shift-6 = ['move-node-to-workspace 6', 'workspace 6']
alt-shift-7 = ['move-node-to-workspace 7', 'workspace 7']
alt-shift-8 = ['move-node-to-workspace 8', 'workspace 8']
alt-shift-9 = ['move-node-to-workspace 9', 'workspace 9']
alt-shift-0 = ['move-node-to-workspace 10', 'workspace 10']
alt-shift-s = ['move-node-to-workspace S', 'workspace S']

alt-shift-f = 'fullscreen'

# See: https://nikitabobko.github.io/AeroSpace/commands#workspace-back-and-forth
alt-tab = 'workspace-back-and-forth'
# See: https://nikitabobko.github.io/AeroSpace/commands#move-workspace-to-monitor
alt-shift-tab = 'move-workspace-to-monitor --wrap-around next'

# See: https://nikitabobko.github.io/AeroSpace/commands#mode
alt-shift-semicolon = 'mode service'

alt-shift-r = 'mode resize'

# Quick swap monitors (useful for reconfiguring workspaces after monitor hotplug)
alt-m = 'mode monitor'

[mode.resize.binding]
h = 'resize width -50'
j = 'resize height +50'
k = 'resize height -50'
l = 'resize width +50'
b = 'balance-sizes'

# See: https://nikitabobko.github.io/AeroSpace/commands#resize
minus = 'resize smart -50'
equal = 'resize smart +50'

enter = 'mode main'
esc = 'mode main'

# New monitor mode for handling monitor-specific actions
[mode.monitor.binding]
# Swap focused workspace to specific monitor
m = ['move-workspace-to-monitor main', 'mode main']
l = ['move-workspace-to-monitor external-left', 'mode main']
r = ['move-workspace-to-monitor external-right', 'mode main']
# Reconfigure monitors
c = ['exec-and-forget ~/.config/aerospace/monitor-setup.sh', 'mode main']
# Save current monitor layout
s = ['exec-and-forget ~/.config/aerospace/save-monitor-layout.sh', 'mode main']
# Apply saved monitor layout
a = ['exec-and-forget ~/.config/aerospace/apply-monitor-layout.sh', 'mode main']
# Exit monitor mode
esc = 'mode main'
enter = 'mode main'

# 'service' binding mode declaration.
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
[mode.service.binding]
esc = ['reload-config', 'mode main']
r = ['flatten-workspace-tree', 'mode main'] # reset layout
#s = ['layout sticky tiling', 'mode main'] # sticky is not yet supported https://github.com/nikitabobko/AeroSpace/issues/2
f = ['layout floating tiling', 'mode main'] # Toggle between floating and tiling layout
backspace = ['close-all-windows-but-current', 'mode main']

alt-shift-h = ['join-with left', 'mode main']
alt-shift-j = ['join-with down', 'mode main']
alt-shift-k = ['join-with up', 'mode main']
alt-shift-l = ['join-with right', 'mode main']

[[on-window-detected]]
if.app-id = 'com.spotify.client'
run = "move-node-to-workspace S"